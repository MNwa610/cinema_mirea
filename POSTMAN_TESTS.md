# Тестовые запросы для Postman

Базовый URL: `http://localhost:7070/api`

---

## 🔐 Аутентификация и пользователи

### 1. Регистрация пользователя
**POST** `/user/registration`

**Headers:**
```
Content-Type: application/json
```

**Body (JSON):**
```json
{
  "username": "testuser",
  "email": "test@example.com",
  "password": "password123"
}
```

**Ожидаемый ответ:** `201 Created`
```json
{
  "message": "Успешная регистрация пользователя",
  "user": {
    "id": 1,
    "username": "testuser",
    "email": "test@example.com",
    "createdAt": "2026-02-20T...",
    "updatedAt": "2026-02-20T..."
  }
}
```

---

### 2. Вход пользователя
**POST** `/user/login`

**Headers:**
```
Content-Type: application/json
```

**Body (JSON):**
```json
{
  "email": "test@example.com",
  "password": "password123"
}
```

**Ожидаемый ответ:** `200 OK`
```json
{
  "token": "eyJhbGciOiJIUzI1NiIsInR5cCI6IkpXVCJ9..."
}
```

**⚠️ ВАЖНО:** Сохраните этот токен для использования в защищенных запросах!

---

### 3. Проверка авторизации (получить профиль)
**GET** `/user/auth`

**Headers:**
```
Authorization: Bearer YOUR_TOKEN_HERE
```

**Ожидаемый ответ:** `200 OK`
```json
{
  "id": 1,
  "username": "testuser",
  "email": "test@example.com",
  "address": null,
  "role": "USER",
  "createdAt": "2026-02-20T...",
  "updatedAt": "2026-02-20T..."
}
```

---

### 4. Получить свой профиль
**GET** `/user/profile`

**Headers:**
```
Authorization: Bearer YOUR_TOKEN_HERE
```

**Ожидаемый ответ:** `200 OK` (аналогично `/user/auth`)

---

### 5. Обновить профиль
**PATCH** `/user/profile/update`

**Headers:**
```
Authorization: Bearer YOUR_TOKEN_HERE
Content-Type: application/json
```

**Body (JSON):**
```json
{
  "username": "newusername",
  "address": "Москва, ул. Примерная, д. 1"
}
```

**Ожидаемый ответ:** `200 OK`
```json
{
  "message": "Профиль пользователя успешно обновлен",
  "user": { ... }
}
```

---

### 6. Получить всех пользователей (только для авторизованных)
**GET** `/user/users`

**Headers:**
```
Authorization: Bearer YOUR_TOKEN_HERE
```

**Ожидаемый ответ:** `200 OK`
```json
[
  {
    "id": 1,
    "username": "testuser",
    "email": "test@example.com",
    ...
  }
]
```

---

### 7. Удалить свой аккаунт
**DELETE** `/user/profile/delete`

**Headers:**
```
Authorization: Bearer YOUR_TOKEN_HERE
```

**Ожидаемый ответ:** `200 OK`
```json
{
  "message": "Пользователь удален успешно"
}
```

---

## 🎬 Кинотеатры

### 8. Получить все кинотеатры
**GET** `/cinema/`

**Ожидаемый ответ:** `200 OK`
```json
[
  {
    "id": 1,
    "name": "Кинотеатр Центральный",
    "address": "Москва, ул. Примерная, д. 10",
    "coordinates": "...",
    "phoneNumber": "+7 (495) 123-45-67",
    "openHours": {
      "monday": "10:00-22:00",
      "tuesday": "10:00-22:00",
      ...
    },
    "createdAt": "2026-02-20T...",
    "updatedAt": "2026-02-20T..."
  }
]
```

---

### 9. Получить кинотеатр по ID
**GET** `/cinema/1`

**Ожидаемый ответ:** `200 OK` (один объект кинотеатра)

---

### 10. Создать кинотеатр (требует авторизации)
**POST** `/cinema/create`

**Headers:**
```
Authorization: Bearer YOUR_TOKEN_HERE
Content-Type: application/json
```

**Body (JSON):**
```json
{
  "name": "Новый кинотеатр",
  "address": "Москва, ул. Новая, д. 5",
  "coordinates": "POINT(37.6173 55.7558)",
  "phoneNumber": "+7 (495) 999-88-77",
  "openHours": {
    "monday": "10:00-23:00",
    "tuesday": "10:00-23:00",
    "wednesday": "10:00-23:00",
    "thursday": "10:00-23:00",
    "friday": "10:00-00:00",
    "saturday": "10:00-00:00",
    "sunday": "10:00-23:00"
  }
}
```

**Ожидаемый ответ:** `201 Created`
```json
{
  "message": "Кинотеатр успешно создан",
  "cinema": { ... }
}
```

**Примечание:** Формат координат `POINT(longitude latitude)` для PostgreSQL Geography типа.

---

### 11. Обновить кинотеатр (требует авторизации)
**PATCH** `/cinema/1/update`

**Headers:**
```
Authorization: Bearer YOUR_TOKEN_HERE
Content-Type: application/json
```

**Body (JSON):**
```json
{
  "name": "Обновленное название",
  "phoneNumber": "+7 (495) 111-22-33"
}
```

**Ожидаемый ответ:** `200 OK`
```json
{
  "message": "Кинотеатр успешно обновлен",
  "cinema": { ... }
}
```

---

### 12. Удалить кинотеатр (требует авторизации)
**DELETE** `/cinema/1/delete`

**Headers:**
```
Authorization: Bearer YOUR_TOKEN_HERE
```

**Ожидаемый ответ:** `200 OK`
```json
{
  "message": "Кинотеатр успешно удален"
}
```

---

## 🎞️ Фильмы

### 13. Получить все фильмы
**GET** `/film/`

**Ожидаемый ответ:** `200 OK`
```json
[
  {
    "id": 1,
    "title": "Интерстеллар",
    "description": "Фантастический фильм о космических путешествиях",
    "posterUrl": "https://example.com/poster.jpg",
    "releaseDate": "2014-11-06",
    "rating": 8.6,
    "genres": ["фантастика", "драма", "приключения"],
    "director": "Кристофер Нолан",
    "actors": ["Мэттью Макконахи", "Энн Хэтэуэй", "Джессика Честейн"],
    "duration": 169,
    "createdAt": "2026-02-20T...",
    "updatedAt": "2026-02-20T..."
  }
]
```

---

### 14. Получить фильм по ID
**GET** `/film/1`

**Ожидаемый ответ:** `200 OK` (один объект фильма)

---

### 15. Создать фильм (требует авторизации)
**POST** `/film/create`

**Headers:**
```
Authorization: Bearer YOUR_TOKEN_HERE
Content-Type: application/json
```

**Body (JSON):**
```json
{
  "title": "Дюна",
  "description": "Эпическая научно-фантастическая сага",
  "posterUrl": "https://example.com/dune-poster.jpg",
  "releaseDate": "2021-10-22",
  "rating": 8.0,
  "genres": ["фантастика", "боевик", "драма"],
  "director": "Дени Вильнёв",
  "actors": ["Тимоти Шаламе", "Ребекка Фергюсон", "Оскар Айзек"],
  "duration": 155
}
```

**Ожидаемый ответ:** `201 Created`
```json
{
  "message": "Фильм успешно создан",
  "film": { ... }
}
```

---

### 16. Обновить фильм (требует авторизации)
**PATCH** `/film/1/update`

**Headers:**
```
Authorization: Bearer YOUR_TOKEN_HERE
Content-Type: application/json
```

**Body (JSON):**
```json
{
  "rating": 8.5,
  "description": "Обновленное описание фильма"
}
```

**Ожидаемый ответ:** `200 OK`
```json
{
  "message": "Фильм успешно обновлен",
  "film": { ... }
}
```

---

### 17. Удалить фильм (требует авторизации)
**DELETE** `/film/1/delete`

**Headers:**
```
Authorization: Bearer YOUR_TOKEN_HERE
```

**Ожидаемый ответ:** `200 OK`
```json
{
  "message": "Фильм успешно удален"
}
```

---

## 📝 Примеры последовательности тестирования

### Сценарий 1: Полный цикл работы с пользователем
1. Регистрация (`POST /user/registration`)
2. Вход (`POST /user/login`) - сохранить токен
3. Проверка авторизации (`GET /user/auth`) - использовать токен
4. Обновление профиля (`PATCH /user/profile/update`) - использовать токен
5. Получение профиля (`GET /user/profile`) - использовать токен

### Сценарий 2: Работа с кинотеатрами
1. Получить все кинотеатры (`GET /cinema/`)
2. Получить конкретный кинотеатр (`GET /cinema/1`)
3. Создать кинотеатр (`POST /cinema/create`) - требуется токен
4. Обновить кинотеатр (`PATCH /cinema/1/update`) - требуется токен

### Сценарий 3: Работа с фильмами
1. Получить все фильмы (`GET /film/`)
2. Получить конкретный фильм (`GET /film/1`)
3. Создать фильм (`POST /film/create`) - требуется токен
4. Обновить фильм (`PATCH /film/1/update`) - требуется токен

---

## ⚠️ Важные замечания

1. **Токен авторизации:** После входа (`/user/login`) сохраните токен и используйте его в заголовке `Authorization: Bearer YOUR_TOKEN` для всех защищенных эндпоинтов.

2. **Формат координат:** Для создания кинотеатра используйте формат `POINT(longitude latitude)` для поля `coordinates`.

3. **Массивы:** Поля `genres` и `actors` в фильмах должны быть массивами строк.

4. **Даты:** Используйте формат `YYYY-MM-DD` для поля `releaseDate`.

5. **Ошибки:** При ошибках API вернет соответствующий HTTP статус код и сообщение об ошибке в JSON формате.

---

## 🔧 Настройка Postman

### Переменные окружения (рекомендуется):
Создайте Environment в Postman со следующими переменными:
- `base_url`: `http://localhost:7070/api`
- `token`: (будет установлен после логина)

Тогда можно использовать запросы как: `{{base_url}}/user/login`

### Автоматическое сохранение токена:
В тестах для запроса `/user/login` добавьте:
```javascript
if (pm.response.code === 200) {
    const jsonData = pm.response.json();
    pm.environment.set("token", jsonData.token);
}
```

Тогда в других запросах используйте: `Authorization: Bearer {{token}}`
